/*!
 * \page msmail_page msmail
 * \anchor msmail_doc
 * \if english
 * \section msmail_intro What is msmail
 * This is a programm.
 * \endif
 *
 * \if russian
 * \section msmail_intro Что такое msmail
 * Сервер сообщений.
 * \section msmail_install Установка msmail
 * Установка осуществляется с помощью автоматической программы установщика.\n
 * Запустите исполняемый файл и следуйте инструкциям установки.\n
 * После установки зарегистрируйте службу msmail:\n
 *  1. Запустите командную строку и перейдите в каталог с установленной программой.\n
 *  2. Запустите регистрацию службы набрав в коммандной строке msmail --install\n
 *  3. Запустите службу набрав в коммандной строке net start msmail\n
 * Перед обновлением убедитесь в том, что служба msmail остановлена.\n
 * \section msmail_files Файлы msmail
 * Перед запуском необходимо создать конфигурационный файл msmail - по умолчанию используется
 * msmail.conf из того же каталога где находится исполняемый файл.\n
 * Можно осуществить это скопировав или переименовав файл msmail.conf.template входящий в инсталяционный пакет.\n
 * Задать имя и расположение файла можно с помощью параметра командной строки -c\n
 * По умолчанию сообщения и ошибки выводятся в файл msmail.log в каталоге программы\n
 * Задать имя и расположение файла вывода сообщений и ошибок можно с помощью параметра командной строки -log\n
 * \endif
 */
