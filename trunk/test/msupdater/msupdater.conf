bind = ;

// affect only under non-windows platforms
daemon = yes;

update {
// interval = 180; in minutes
  url = http://system;
//  proxy = proxy:3128; // http proxy - username:password@hostname:port
// resume = true;
// storage = ; // directory for store updates
}
